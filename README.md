# NDL OS

Site NDL OS — version exportée depuis le projet actuel.

## Modifier
- Ouvre ce dépôt dans Cursor ou VS Code.
- Les pages sont des fichiers `index.html`.
- Les images sont dans `assets/`.

Le site est statique : HTML, CSS et JavaScript.